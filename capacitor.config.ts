import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.zapsys.listadecompras',
  appName: 'Lista de Compras',
  webDir: 'dist'
};

export default config;
